# ─── config/.env.example ─────────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env — it contains secrets.
#
# Loading order: config/.env then config/.env.dev.local (loaded together via dotenv-command)
# Create config/.env.dev.local for machine-local overrides (also gitignored).
# ────────────────────────────────────────────────────────────

# restic repository
RESTIC_PASSWORD=change-me-to-a-strong-password
RESTIC_REPOSITORY=sftp:user@host:/path/to/restic-repo

# Local repository example (uncomment and adjust):
# RESTIC_REPOSITORY=/Volumes/BackupDrive/restic-repo

# S3 / B2 example (uncomment and adjust):
# RESTIC_REPOSITORY=s3:bucket-name:/path
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Hostname tag — set explicitly so snapshots are portable across machines
HOSTNAME=my-machine

# Snapshot tag — identifies backups from this project when filtering or forgetting
# Choose a name you'll recognise, e.g. workstation, home-server, my-backup
BACKUP_TAG=my-backup

# Parent directory — cd here before backup so restic stores relative paths
# Example: if BACKUP_PARENT=/home/user, BACKUP_SOURCE_DIR="data projects" stores data/* and projects/*
# Leave unset (empty) to use absolute paths in the snapshot
# BACKUP_PARENT=/home/user

# ─── videodl ────────────────────────────────────────────
# Download root directory — all videos and subtitles go here
# VIDEO_DL_ROOT=/Volumes/Media/yt-dlp

# Subtitle language priority, English first
# Format: language codes, comma separated
SUBTITLE_LANGS=en,zh-Hans,ja,ko

# Proxy (dedicated var, won't interfere with other programs)
# VIDEO_DL_PROXY=http://127.0.0.1:7890

# YouTube cookies (optional, for age-restricted or member-only content)
# Generated via: just videodl::cookies
# Or set explicitly:
# COOKIES_FROM=browser_name    # e.g. firefox, chrome, safari, brave
# COOKIES_FILE=/path/to/cookies.txt

# ─── backup ───────────────────────────────────────────────
# Directory(ies) to back up — space-separated
# When BACKUP_PARENT is set:  relative paths under BACKUP_PARENT
# When BACKUP_PARENT is unset: absolute paths
# Single:   BACKUP_SOURCE_DIR=/path/to/data
# Multiple: BACKUP_SOURCE_DIR="/path/to/a /path/to/b /path/to/c"
BACKUP_SOURCE_DIR=/path/to/your-important-data

# ─── notify ───────────────────────────────────────────────
# Telegram Bot token — get from @BotFather
# NOTIFY_TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# Recipient chat ID — get from @userinfobot
# NOTIFY_TELEGRAM_CHAT_ID=123456789

# Telegram API proxy (optional, for GFW environments)
# NOTIFY_TELEGRAM_PROXY=http://127.0.0.1:7890

# Default notification channel (comma-separated: telegram, stdout)
NOTIFY_DEFAULT_CHANNEL=telegram

# Audit log path (default: modules/notify/notify.log)
# NOTIFY_LOG_FILE=

# Output tail lines for watch command (default: 30)
NOTIFY_TAIL_LINES=30

# ─── finance ──────────────────────────────────────────────
# Path to main.beancount (external, must NOT be inside this repo)
# FINANCE_BEANCOUNT_FILE=/path/to/your/main.beancount

# Fava server port (default: 5500)
# FINANCE_FAVA_PORT=5500

# Proxy for uv (optional, used by just finance::setup to download packages)
# FINANCE_PROXY=http://127.0.0.1:7890
